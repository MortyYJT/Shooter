package shooter;

/**
 * Horizontal facing direction for sprites that can be mirrored.
 */
public enum Direction {
    /** Sprite faces toward the left side of the screen. */
    LEFT,

    /** Sprite faces toward the right side of the screen. */
    RIGHT
}
